plugins {
    id 'java-gradle-plugin'
    id "de.materna.gsb.gsb-publishing" version "0.6.2"
}

dependencies {
    implementation "io.freefair.gradle:aspectj-plugin:6.4.3"
}

gradlePlugin {
    plugins {
        tracelog {
            id = 'de.materna.gsb.tracelog'
            implementationClass = 'de.materna.gsb.gradle.tracelog.TracelogPlugin'
        }
    }
}